- defaults:
    links:
        relink: true

- clean: ['~']

- link:
    ~/.vim:
    ~/.vimrc:
    ~/.zshrc:
    ~/.gitignore_global:

- shell:
    - ['git submodule update --init --recursive', 'Installing submodules']
