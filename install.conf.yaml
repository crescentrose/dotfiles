- defaults:
    links:
        relink: true

- clean: ['~']

- link:
    ~/.vim:
    ~/.vimrc:
    ~/.zshrc:
    ~/.gitignore_global:
    ~/.gemrc:
    ~/.gitconfig:

- shell:
    - ['git submodule update --init --recursive', 'Installing submodules']
    - ['brew install rbenv', 'Installing rbenv']
